# HandoffAI backend

FastAPI service that decodes nurse handoff notes with NVIDIA Nemotron, persists results to Postgres (Supabase), and exposes APIs for handoffs, room history, and verification.

See the repository `buildplan.md` for the full configuration cheat sheet, Render deploy notes, and verification curls.
